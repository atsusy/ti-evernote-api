#import "TiProxy.h"
#import "EDAM<%= @basename %>.h"
#import "JpMasuidriveTiEvernoteapiClientProxy.h"

@interface <%= s.proxy_class_name %> : JpMasuidriveTiEvernoteapiClientProxy {
@private
    <%= s.class_name %>* client;
}
@property (nonatomic, retain) <%= s.class_name %>* client;

- (id)initWithUri: (NSString*)uri;

@end
